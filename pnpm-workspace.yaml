packages:
  - "languages/*"
# esbuild (used by tsup) ships a build script; pnpm blocks these by default.
# Approve it so the build runs and pnpm stops flagging it as ignored.
allowBuilds:
  esbuild: true
onlyBuiltDependencies:
  - esbuild
