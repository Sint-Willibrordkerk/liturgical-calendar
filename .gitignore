.env

node_modules
dist
.turbo
.yarn/install-state.gz

.divinum-officium
test_output.yml

# The Mass propers, generated by `pnpm copy-mass-propers` from the pipeline
# output. Megabytes per language, so they are published to npm rather than
# committed; the translations beside them are hand-maintained and tracked.
languages/*/assets/mass-propers/
languages/*/dist/